import { Meta, Story, Canvas, ArgsTable } from "@storybook/addon-docs";
import { SectionContainer } from "./index";

<Meta
  title="Components/SectionContainer"
  component={SectionContainer}
  argTypes={{
    background: {
      control: {
        type: "color"
      },
    },
    gap: {
      control: {
        type: "number",
        min: 0,
        step: 5,
      },
      table: {
        category: "FLEX",
      },
    },
    flexDirection: {
      control: {
        type: "select",
      },
      name: "flex direction",
      table: {
        category: "FLEX",
      },
    },
    align: {
      control: {
        type: "select",
      },
      name: "align-items",
      table: {
        category: "FLEX",
      },
    },
    justify: {
      control: {
        type: "select",
      },
      name: "justify-content",
      table: {
        category: "FLEX",
      },
    },
    styles: {
      control: {
        type: "object",
      },
      name: "Custom styles",
      table: {
        category: "OTHERS",
      },
    },
    ref: {
      control: {
        type: "disabled",
      },
      name: "ref",
      table: {
        category: "OTHERS",
      },
    },
  }}
/>

# SectionContainer

This component is a wrapper using the section HTML tag section that you can use everytime you start to develop a new section in your project, saving some time.

```js
import { Container } from "domeui-components";
```

<ArgsTable of={SectionContainer} />

## Default

<Canvas columns={2} withSource="open" withToolbar>
  <Story
    name="Default"
    args={{
      ...SectionContainer.defaultProps,
    }}
  >
    {(args) => <SectionContainer {...args} />}
  </Story>
</Canvas>

<ArgsTable story="Default" />
